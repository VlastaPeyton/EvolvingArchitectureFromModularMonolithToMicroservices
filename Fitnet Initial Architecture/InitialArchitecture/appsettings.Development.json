{
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.AspNetCore": "Warning"
        }
    },
    "ConnectionStrings": {
        "Passes": "Host=localhost;Port=5432;Database=FitnetDb;Username=postgres;Password=postgres",
        "Contracts": "Host=localhost;Port=5432;Database=FitnetDb;Username=postgres;Password=postgres",
        "Reports": "Host=localhost;Port=5432;Database=FitnetDb;Username=postgres;Password=postgres",
        "Offers": "Host=localhost;Port=5432;Database=FitnetDb;Username=postgres;Password=postgres"
        // App u localhost koristi 5432, a isti taj, jer mu je default, koristi Postgres Docker, jer baza mi je Docker i kad je app localhost i kad je app in Docker
    }
}
